/*@isTest*/
public class BATCH_WS04_TEST {
 
 	/*@isTest
 	static void processToBatchTest() {
 		
 		List<Lead> leads = [SELECT Id FROM Lead];
    	System.assert(leads.isEmpty());
    	
 		Test.startTest();
 		System.enqueueJob(new BATCH_WS04());
 		Test.stopTest();
 		
 		leads = [SELECT Id FROM Lead];
    	System.assert(leads.size() == 2);
 	}*/  
}